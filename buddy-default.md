# Buddy default database

The file `buddy-default.sqlite` is an empty default database. Its only users are:

| Name | Password | Role |
|------|----------|------|
| root | root     | 0    |
| kruse| IloveC   | 0    |
| hofi | urlaub   | 1    |
